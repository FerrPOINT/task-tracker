services:
  postgres:
    image: postgres:17.6-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-tasktracker}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-[CHANGE_ME]}
      POSTGRES_DB: ${POSTGRES_DB:-tasktracker}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-3457}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tasktracker} -d ${POSTGRES_DB:-tasktracker}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:8.0-alpine
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    environment:
      # config crate format: TASKTRACKER_ + SECTION__KEY (separator "__", prefix_separator "_")
      TASKTRACKER_DATABASE__URL: ${TASKTRACKER_DATABASE__URL}
      TASKTRACKER_SERVER__ADDRESS: 0.0.0.0
      TASKTRACKER_SERVER__PORT: 3456
      TASKTRACKER_JWT_SECRET: ${TASKTRACKER_JWT_SECRET:-[CHANGE_ME]}
      # Rate limits are configurable; defaults are production-safe (auth 5/15s, api 60/60s).
      TASKTRACKER_SERVER__AUTH_RATE_BURST: ${TASKTRACKER_SERVER__AUTH_RATE_BURST:-5}
      TASKTRACKER_SERVER__AUTH_RATE_PERIOD_SECS: ${TASKTRACKER_SERVER__AUTH_RATE_PERIOD_SECS:-15}
      TASKTRACKER_SERVER__GENERAL_RATE_BURST: ${TASKTRACKER_SERVER__GENERAL_RATE_BURST:-60}
      TASKTRACKER_SERVER__GENERAL_RATE_PERIOD_SECS: ${TASKTRACKER_SERVER__GENERAL_RATE_PERIOD_SECS:-60}
      RUST_LOG: ${RUST_LOG:-info}
    ports:
      - "3456:3456"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3456/api/v1/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
    ports:
      - "19877:80"
    depends_on:
      backend:
        condition: service_healthy

  traefik:
    image: traefik:v3.4
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
    ports:
      - "8080:80"
      - "8081:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    profiles:
      - traefik

volumes:
  postgres_data:
  redis_data:
